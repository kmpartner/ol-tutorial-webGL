language: node_js
node_js:
  - 8

script:
    - npm run build

services:
  - docker
  
before_install:
  - docker build -t kmpartner/ol-tutorial-webgl .
  - docker run -d -p 127.0.0.1:80:80 kmpartner/ol-tutorial-webgl /bin/sh -c "cd /root/kmpartner/build; ls /root/kmpartner/;"
#   - docker run -d -p 127.0.0.1:80:4567 carlad/sinatra /bin/sh -c "cd /root/sinatra; bundle exec foreman start;"
  - docker ps -a
  
